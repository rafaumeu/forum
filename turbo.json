{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "test": { "dependsOn": ["build"] },
    "test:coverage": { "dependsOn": ["build"] },
    "lint": {},
    "generate:api": { "cache": false },
    "dev": { "cache": false, "persistent": true }
  }
}
